# Datasets

- `berlin52.tsp` - TSPLIB instance, 52 locations in Berlin, best known optimum 7542.
- `montenegro` - 21 cities, hardcoded in `annealing.tsp.datasets`.
- `random` - generated via `load_random(count, seed)`.
